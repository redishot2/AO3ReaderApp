//
//  CustomColors.swift
//  ReaderApp
//
//  Created by Natasha Martinez on 3/3/25.
//

import SwiftUI

extension Color {
    struct System {
        let white = Color("systemWhite")
        let black = Color("systemBlack")
    }
    struct Set1 {
        let blue = Color("set1-blue")
        let blueDark = Color("set1-blueDark")
        let pink = Color("set1-pink")
        let pinkDark = Color("set1-pinkDark")
        let green = Color("set1-green")
        let greenDark = Color("set1-greenDark")
        let yellow = Color("set1-yellow")
        let yellowDark = Color("set1-yellowDark")
    }
}
